# 12minute.upk 文件分析报告

## 1. 文件基本信息

| 属性 | 值 |
|------|-----|
| 文件名 | 12minute.upk |
| 文件大小 | 4,276,224 字节（约 4.08 MB） |
| 文件类型 | 二进制数据文件 |

## 2. 文件格式分析

### 2.1 数值特征

通过对文件的二进制分析，发现以下特征：

- 文件共包含 **2,138,112 个 16 位（2 字节）字**
- 所有数值均落在 **0 至 4095** 的范围内，即完全符合 **12 位精度**数据的特征
- 数值均值约为 3730.9，标准差约为 1075.9
- 末尾区域大量填充值为 0x0FFF（4095），疑为录音结束后的填充数据

### 2.2 推断格式：舱音记录仪（CVR）原始音频数据

根据文件名"12minute"及数值特征，本文件极可能为 **驾驶舱语音记录仪（CVR）12 分钟录音的原始解包数据（unpacked data）**，即从 CVR 磁带或固态存储器中提取的原始 PCM 音频数据。

**推算依据：**

- 文件时长验证：2,138,112 样本 ÷ 720 秒 ≈ **2969.6 样本/秒（总采样率）**
- 波音 737-800 典型 CVR 配置为 **4 通道**录音（机长耳机麦克风、副驾驶耳机麦克风、驾驶舱环境麦克风、第三套无线电音频），每通道采样率约 **742 Hz**
- 4 通道 × 742 样本/秒 × 720 秒 ≈ 2,138,112 样本，**与实际文件完全吻合**
- 采样值均落在 0–4095 范围，符合 **12 位偏移二进制编码（offset binary）**的 CVR 音频格式，其中静音电平对应约 2048（中间值）

### 2.3 通道解析推测

若上述推断成立，4 个通道可能依次交错存储（interleaved），排列格式为：

```
[Ch1_样本0][Ch2_样本0][Ch3_样本0][Ch4_样本0][Ch1_样本1][Ch2_样本1]...
```

各通道对应关系（典型 NTSB CVR 格式）：

| 通道 | 对应内容 |
|------|----------|
| 通道 1 | 机长区域麦克风 |
| 通道 2 | 副驾驶区域麦克风 |
| 通道 3 | 驾驶舱环境麦克风（顶部） |
| 通道 4 | 高频无线电音频 |

## 3. 事故背景

本文件来源于 MU5735 事故调查，事故概况如下：

- **航班**：中国东方航空 MU5735 次
- **机型**：波音 737-800，注册号 B-1791
- **事故日期**：2022 年 3 月 21 日
- **事故时间**：约 14:20 CST（北京时间）/ 06:20 UTC
- **事故地点**：广西梧州藤县上空

CVR 按规定录制最近 30 分钟（部分型号为 2 小时）的驾驶舱声音。本文件截取了事故前 **最后 12 分钟**的录音内容，与飞行数据记录仪（FDR）数据的记录时段（08:03:20Z 至 08:16:17Z，约 13 分钟）高度吻合。

## 4. 技术局限性说明

本分析仅基于文件二进制结构的数学推断，**不能**在没有专业 CVR 解码工具和相应密钥（部分 CVR 系统对音频加密）的情况下还原音频内容。

CVR 原始数据的解码需要：
1. 专业的 NTSB/CAAC CVR 解码工作站
2. 具体型号 CVR 的格式规范（如 Honeywell SSFDR 格式文档）
3. 如有加密，还需相应密钥

## 5. 结论

`12minute.upk` 文件很可能是 MU5735 事故 CVR 的最后 12 分钟原始音频数据，以 4 通道、每通道约 742 Hz 的 12 位 PCM 格式存储。文件末尾存在大量 0x0FFF 填充值，符合录音结束后固态存储介质的典型特征。该文件是本次事故调查中最为敏感和重要的证据之一。
